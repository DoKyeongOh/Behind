CREATE TABLE `ACCOUNT` (
	`account_no` INT NOT NULL AUTO_INCREMENT PRIMARY KEY REFERENCES USER(account_no),
	`id` VARCHAR(15) NOT NULL UNIQUE,
	`password` TEXT NOT NULL,
	`email` VARCHAR(30) NOT NULL
);