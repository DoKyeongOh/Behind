CREATE TABLE `REPLY_LOG` (
	`reply_log_no` BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`logging_date` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	`action_type` VARCHAR(10) NULL,
	`reply_no` bigint NULL,
	`account_no` INT NULL REFERENCES PROFILE(account_no)
);